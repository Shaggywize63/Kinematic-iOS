app_identifier("com.shaggywize63.kinematic")

# Apple Developer Program team. App Store Connect authentication is done with
# the API key configured in the Fastfile (app_store_connect_api_key), so no
# Apple ID / password / 2FA is needed for headless CI. This must be the team
# the API key is bound to — every cert / profile / App ID lives under it.
team_id("WQ25J762R4")
